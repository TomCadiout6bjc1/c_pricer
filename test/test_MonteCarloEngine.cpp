#include <iostream>

#include "Model.h"
#include "Payoff.h"

int main()
{


}

/*
* // Using enum
enum Color {
    Red,
    Green,
    Blue
};

Color color = Red;  // Directly accessible in the enclosing scope

// Using enum class
enum class Animal {
    Dog,
    Cat,
    Bird
};

Animal animal = Animal::Dog;  // Accessed using the scope resolution operator

*/

